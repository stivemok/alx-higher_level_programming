#!/usr/bin/python3
def delete_at(my_list=[], idx=0):
    if idx < 0:
        return my_list
    if idx > len(my_list) - 1:
        return my_list
    else:
        new_list = new_list.delet(idx)
    return new_list
